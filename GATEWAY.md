# بوابة المكتبة — API واحد لكل البرامج

المكتبة صارت تستقبل طلبات API بصيغة OpenAI وتحوّلها إلى نماذج OpenRouter المجانية.
أي برنامج يعرف يتكلّم مع OpenAI يقدر يشتغل على المكتبة بدون تعديل كوده — تغيّر
العنوان والمفتاح فقط.

هذا خيار **إضافي** بجانب ACP، وليس بديلاً عنه.

---

## 1. التشغيل — ثلاث خطوات

1. افتح المكتبة → زر **النماذج** → الصق مفتاح OpenRouter (يُحفظ على جهازك فقط).
2. اضغط **قِس النماذج** — تنتظر حتى ينتهي القياس (يقيس واحداً تلو الآخر).
3. اضغط **أنشئ مفتاحاً** في صندوق «بوابة المكتبة» وانسخ المفتاح.

بعدها استخدم:

```
Base URL : http://127.0.0.1:4500/api/gateway/v1
API Key  : mk-...  (المفتاح الذي أنشأته)
Model    : auto
```

---

## 2. `auto` — الاختيار الذكي

اكتب `auto` بدل اسم النموذج، وتختار المكتبة الأفضل حسب **القياس الفعلي**، لا حسب
الاسم أو الشهرة. تقدر تطلب مهارة محدّدة:

| الاسم            | المعنى                                        |
|------------------|-----------------------------------------------|
| `auto`           | الأعلى ترتيباً بشكل عام                        |
| `auto:arabic`    | نجح فعلاً في اختبار العربية                    |
| `auto:code`      | نجح في اختبار الكود                            |
| `auto:json`      | يرجّع JSON نظيفاً بلا كلام زائد                 |
| `auto:reasoning` | نجح في اختبار الحساب                           |
| `auto:arabic,json` | نجح في الاثنين معاً                          |

لو كتبت اسم نموذج صريح، **يُستخدم هو أولاً دائماً** — البوابة لا تبدّل اختيارك من
تلقاء نفسها. البدائل تجي بعد فشله فقط.

### السلسلة الاحتياطية
النماذج المجانية تُرفَض كثيراً بحدّ الطلبات. لو رفض النموذج الأول، تجرّب البوابة
التالي في الترتيب (حتى أربعة)، وتُبعِد النموذج الفاشل خمس دقائق حتى لا ترتطم
بنفس الحدّ في كل طلب. الرد يوصلك، ومعه في `maktaba.routed_to` اسم النموذج الذي
أجاب فعلاً.

---

## 3. أمثلة

### curl
```bash
curl http://127.0.0.1:4500/api/gateway/v1/chat/completions \
  -H "Authorization: Bearer mk-YOUR-TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"model":"auto","messages":[{"role":"user","content":"مرحبا"}]}'
```

### Python (مكتبة openai الرسمية)
```python
import os
from openai import OpenAI

# ضع مفتاحك في متغيّر بيئة، لا في الكود.
client = OpenAI(
    base_url="http://127.0.0.1:4500/api/gateway/v1",
    api_key=os.environ["MAKTABA_GATEWAY_KEY"],
)

r = client.chat.completions.create(
    model="auto:arabic",
    messages=[{"role": "user", "content": "اكتب سطراً واحداً عن الرياض"}],
)
print(r.choices[0].message.content)
```

### Node
```js
const res = await fetch('http://127.0.0.1:4500/api/gateway/v1/chat/completions', {
  method: 'POST',
  headers: { 'Authorization': 'Bearer mk-YOUR-TOKEN', 'Content-Type': 'application/json' },
  body: JSON.stringify({ model: 'auto', messages: [{ role: 'user', content: 'hi' }] })
});
const data = await res.json();
console.log(data.choices[0].message.content, '←', data.maktaba.routed_to);
```

---

## 4. النقاط المتاحة

| الطريق | الوظيفة |
|--------|---------|
| `POST /api/gateway/v1/chat/completions` | محادثة (بروتوكول OpenAI) |
| `GET  /api/gateway/v1/models` | قائمة النماذج مع ترتيبها المقيس |
| `GET  /api/gateway/status` | جاهزية البوابة والنموذج الأفضل حالياً |
| `POST /api/gateway/token` | إنشاء مفتاح (`{"revoke":true}` للإلغاء) |
| `GET  /api/gateway/usage` | كم طلباً خدمت البوابة وأي نموذج |
| `POST /api/models/bench/run` | تشغيل القياس (`onlyFailed` لإعادة الفاشل فقط) |
| `GET  /api/models/scores` | كل النتائج مرتّبة |
| `GET  /api/models/best?arabic=1` | أفضل نموذج لمهارة معيّنة |

---

## 5. القياس — كيف يُحدَّد «الأفضل»

خمسة اختبارات لكل نموذج، كلها لها إجابة صحيحة معروفة مسبقاً:

| الاختبار | الوزن | ما يقيسه |
|----------|-------|----------|
| `follows-instruction` | 3 | «أجب بكلمة واحدة: OK» — هل يلتزم؟ |
| `arabic` | 3 | «ما عاصمة مصر؟» بالعربية — الجواب عربي وصحيح |
| `reasoning` | 2 | مسألة حساب بسيطة، الجواب رقم واحد |
| `json` | 2 | يرجّع JSON مطابقاً بلا كلام زائد |
| `code` | 2 | سطر JavaScript يعكس نصاً |

الترتيب = `0.6 × الدقّة + 0.3 × الاستجابة + 0.1 × السرعة`.

الدقّة هي الوزن الأهم لأن نموذجاً سريعاً بجواب خاطئ لا ينفع. السرعة تفصل فقط بين
نموذجين متساويين في الجواب.

**نموذج لا يستجيب في أكثر من نصف الاختبارات لا يُختار تلقائياً أبداً** مهما كانت
درجته حين يردّ.

### ثلاث ملاحظات من القياس الفعلي

- **الحدّ اليومي هو القيّد الحقيقي.** أول تشغيل أجاب 40 اختباراً ورُفض 42. ظننتُ
  أن السبب سرعة الطلبات، فأضفت فاصلاً 3.5 ثانية — وتكرّر الرفض. السبب الفعلي جاء
  من الرسالة نفسها بعد ما أصلحتُ إخفاءها:
  `Rate limit exceeded: free-models-per-day`.
  الرقم الوحيد الذي ذكرته OpenRouter صراحةً هو **1000 طلب يومياً مع رصيد 10$**.
  أما حدّ الحساب المجاني فلا تنشره: `limit` و `limit_remaining` كلاهما `null`،
  ولا تُرجع أي ترويسة `x-ratelimit`. المقيس عندنا: **نجح 62 طلباً على الأقل
  في يوم واحد** قبل أن يُرفض الباقي. فالحدّ موجود، وأعلى من 60، ورقمه الدقيق
  غير معلوم لنا. قياس 20 نموذجاً يحتاج 100 طلب — أي أكثر من الحصّة.
- **القياس يتوقّف عند الحصّة ولا يكتب أصفاراً.** كل طلب بعد نفاد الحصّة يُرفض بنفس
  السبب، فلو استمرّ القياس لأعطى صفراً لنماذج ممتازة. الآن يتوقّف، ويحتفظ بما قيس
  فعلاً، ويكمل في يوم آخر. والبطاقات القديمة التي كانت «رفضاً» لا «قياساً» حُذفت.
- **نماذج غير نصّية**: الفري فيه مولّدات موسيقى وصور. `google/lyria-3` يعلن أنه
  يُخرِج نصاً وصوتاً، وردّ على «أجب بكلمة واحدة» بـ `[13.7:17.1] OK` — سطر أغنية
  بتوقيت، ونجح في الاختبار. الآن يُقاس فقط ما يُخرِج نصاً وحده.

### الحصّة اليومية — كيف تتعايش معها

| الحالة | العدد |
|--------|-------|
| حصّة الحساب المجاني | **غير منشورة** — قِسنا 62 طلباً ناجحاً في يوم |
| مع رصيد 10$ | 1000 طلباً يومياً (ذكرتها OpenRouter نصّاً) |
| قياس نموذج واحد | 5 طلبات |
| قياس كل النماذج | ~100 طلب (يومان) |

المهمة الأسبوعية تقيس **5 نماذج فقط** في كل مرة (25 طلباً) حتى تبقى حصّة
لاستخدامك أنت. الرقم 5 اختيار متحفّظ لأن الحدّ نفسه غير معلوم، لا حساب دقيق له. ولو نفدت الحصّة، البوابة ترجّع خطأ واضحاً باسمه لا «كل النماذج فشلت».

## 6. الأمان

- **البوابة محلية فقط.** الخادم يستمع على `127.0.0.1` ويرفض أي طلب اسم مضيفه ليس محلياً.
- **المفتاح مطلوب** رغم ذلك: أي شيء يعمل على جهازك يقدر يصل `127.0.0.1` — ومنه المتصفح.
- **مفتاح OpenRouter لا يغادر جهازك ولا يصل المتصفح.** البرنامج الذي يستخدم البوابة
  يحمل مفتاح المكتبة فقط.
- **مفتاح جديد يُبطل القديم.** أي برنامج يستخدم القديم يتوقف حتى تحدّثه.
- ملفا `gateway-token.json` و `openrouter-key.json` خارج git.

---

## 7. حدود معروفة

- **البثّ (streaming) ليس تدريجياً.** لو طلبت `stream: true` يوصلك الرد كاملاً في
  قطعة واحدة بصيغة SSE صحيحة. البرامج التي تشترط البثّ تشتغل، لكن الكلمات لا
  تظهر واحدة واحدة. البثّ الحقيقي يعني التخلّي عن السلسلة الاحتياطية، وهي الأنفع.
- **لا يدعم `tools` / function calling** حالياً.
- **النماذج المجانية تتغيّر.** أعِد القياس كل فترة حتى يبقى `auto` صادقاً.
- **الحصّة اليومية مشتركة** بين القياس والبوابة والمحادثة في الموقع. القياس الكامل
  يستهلكها، فشغّله حين لا تحتاجها.
